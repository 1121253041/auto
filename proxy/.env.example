# CORS Proxy Server Configuration

# Server port
PORT=3001

# CORS origin (your frontend URL)
CORS_ORIGIN=http://localhost:3000

# Allowed domains (comma-separated, use * for all domains - not recommended for production)
ALLOWED_DOMAINS=*

# Example for production:
# ALLOWED_DOMAINS=example.com,api.example.com,another-domain.com